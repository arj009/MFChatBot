# Phase 0+
pyyaml>=6.0
python-dotenv>=1.0.1

# Phase 1 — corpus curation (HTTP reachability)
requests>=2.31.0

# Phase 2.2 — HTML parse
beautifulsoup4>=4.12.0

# Phase 3 — Indexing & Retrieval
sentence-transformers>=2.2.2
chromadb>=0.4.15
tqdm>=4.65.0

# Phase 4-5 — Generation, API, and serving
groq>=0.9.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
